const API='http://localhost:3000/api'; function updateClock(){const n=new Date();const c=document.getElementById('clock');if(c)c.textContent=String(n.getHours()).padStart(2,'0')+':'+String(n.getMinutes()).padStart(2,'0')+':'+String(n.getSeconds()).padStart(2,'0');} async function loadHistory(){try{const res=await fetch(API+'/trips/history');const data=await res.json();const tbody=document.getElementById('history-body');if(!data.data.length){tbody.innerHTML='<tr><td colspan=7 style=text-align:center;padding:30px>No completed trips yet</td></tr>';return;}tbody.innerHTML=data.data.map(t=>'<tr><td>#'+t.id+'</td><td>'+t.type+'</td><td>'+t.driver_name+'</td><td>'+t.route_from+' to '+t.route_to+'</td><td>'+t.passengers_count+'</td><td style=color:#10b981;font-weight:bold>'+Number(t.total_income).toLocaleString()+' IQD</td><td><button class=btn onclick=viewPassengers('+t.id+')>Passengers</button></td></tr>').join('');}catch(e){console.log(e);}} async function viewPassengers(id){const res=await fetch(API+'/passengers/trip/'+id);const data=await res.json();alert('Passengers:
'+data.data.map((p,i)=>(i+1)+'. '+p.name+' '+p.passport).join('
'));} document.addEventListener('DOMContentLoaded',()=>{updateClock();setInterval(updateClock,1000);loadHistory();});