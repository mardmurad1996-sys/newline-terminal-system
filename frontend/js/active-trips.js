const API='http://localhost:3000/api'; function updateClock(){const n=new Date();const c=document.getElementById('clock');if(c)c.textContent=String(n.getHours()).padStart(2,'0')+':'+String(n.getMinutes()).padStart(2,'0')+':'+String(n.getSeconds()).padStart(2,'0');} async function loadActive(){try{const res=await fetch(API+'/trips/active');const data=await res.json();const tbody=document.getElementById('trips-body');if(!data.data.length){tbody.innerHTML='<tr><td colspan=7 style=text-align:center;padding:30px>No active trips</td></tr>';return;}tbody.innerHTML=data.data.map(t=>'<tr><td>#'+t.id+'</td><td>'+t.type+'</td><td>'+t.driver_name+'</td><td>'+t.route_from+' to '+t.route_to+'</td><td>'+t.passengers_count+'</td><td><button class=btn onclick=completeTrip('+t.id+')>Complete</button> <button class=btn onclick=viewPassengers('+t.id+')>Passengers</button></td></tr>').join('');}catch(e){console.log(e);}} async function completeTrip(id){if(!confirm('Complete this trip?'))return;await fetch(API+'/trips/'+id+'/complete',{method:'PUT',headers:{'Content-Type':'application/json'}});loadActive();} async function viewPassengers(id){const res=await fetch(API+'/passengers/trip/'+id);const data=await res.json();alert('Passengers:
'+data.data.map((p,i)=>(i+1)+'. '+p.name+' '+p.passport).join('
'));} function showNotif(m,t){const n=document.createElement('div');n.className='notification notif-'+(t||'info');n.textContent=m;document.body.appendChild(n);setTimeout(()=>n.remove(),3000);} document.addEventListener('DOMContentLoaded',()=>{updateClock();setInterval(updateClock,1000);loadActive();setInterval(loadActive,5000);});